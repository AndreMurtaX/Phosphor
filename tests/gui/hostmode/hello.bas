rem A program with no window at all. It exists to prove that the ONE binary is
rem still a console interpreter -- on a machine with a graphical session and on
rem one without, identically.
println "console ok"
